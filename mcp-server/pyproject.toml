[project]
name = "allratestoday-mcp-currency-server"
version = "0.1.0"
description = "Currency conversion MCP server powered by the AllRatesToday exchange rate API"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastmcp==2.11.3",
    "httpx>=0.27.0",
]
